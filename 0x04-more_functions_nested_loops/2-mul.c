#include "main.h"
/**
 * mul – it multiplies 2 integers
 * @a: is the first integer
 * @b: is the second integer
 * Return: is the addition of multiplications done
 */

int mul(int a, int b)
{
	return (a * b);
}
